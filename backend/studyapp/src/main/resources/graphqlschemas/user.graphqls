type User {
    id: ID!
    userName: String
    fullName: String
    groups: [Group]
    managedGroups: [Group]
    createdTasks: [Task]
    createdTests: [Test]
}


type Query {
    getUserById(id: ID!): User
}