
type Group {
    id: ID!
    name: String
    users: [User]
    admins: [User]
    userIds: [String]
}

extend type Query {
    group(id: ID!): Group
    userGroups(id: ID!): [Group]
    groupDTO(id: ID!): Group
}