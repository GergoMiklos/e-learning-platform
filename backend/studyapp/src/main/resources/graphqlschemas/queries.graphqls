type Query {
    user(userId: ID!): User
    group(groupId: ID!): Group
    test(testId: ID!): Test
    searchTasks(searchText: String, page: Int!): TaskSearchResult!
    task(userId: ID!, testId: ID!): Task
}